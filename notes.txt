maybe add this at some point:
renderer.toneMapping = THREE.ACESFilmicToneMapping;
renderer.toneMappingExposure = 1.0;


// 1. Make a massive cube that covers your entire city model
const hazeGeo = new THREE.BoxGeometry(10000, 10000, 10000);

// // 2. The magic material
// const hazeMat = new THREE.MeshBasicMaterial({
//     color: 0x001c42,
//     transparent: true,
//     opacity: 0.1,          // Extremely low strength (just like your Blender emission!)
//     depthWrite: false,      // Crucial: stops the box from hiding your buildings
//     blending: THREE.AdditiveBlending, // Makes it act like glowing light
//     side: THREE.BackSide    // Crucial: You are rendering the INSIDE walls of the box
// });

// const hazyCube = new THREE.Mesh(hazeGeo, hazeMat);
// // Position it so your city and camera are completely inside it
// hazyCube.position.set(0, 0, 0);
// scene.add(hazyCube);



    // gltf.scene.traverse((child) => {
    //     if (child.isMesh && child.material) {

    //         // 1. Set your intensity back to a normal, good-looking number!
    //         child.material.emissiveIntensity = 3.0;

    //         // 2. The Magic Trick: Modify the core shader
    //         child.material.onBeforeCompile = (shader) => {
    //             // We find the exact moment Three.js applies the fog...
    //             shader.fragmentShader = shader.fragmentShader.replace(
    //                 '#include <fog_fragment>',
    //                 `
    //                 // Let the fog darken the building's walls normally
    //                 #include <fog_fragment>

    //                 // AFTER the fog is applied, force the raw emission color back onto the screen.
    //                 // Because your walls have 0 emission on the texture map, they stay hidden in the fog.
    //                 // But the windows will punch through at 100% brightness, no matter the distance!
    //                 gl_FragColor.rgb += totalEmissiveRadiance;
    //                 `
    //             );
    //         };
    //     }
    // });
